language: python

sudo: enabled
# command to install dependencies
install:
  - pip install selenium
  
 
# command to run tests
script: pytest test_forks.py

before_script: cd Staging
 pip install blocks


    
   
